/*
2. Considere o trecho de código abaixo e descreva o que acontece com a execução
do código abaixo:

let a : number[] = [1,2,3,4,5];
for (let i : number = 0; i <= a.length; i++) {
    console.log(a[i]);
}

- Na primeira linha é criado um array de números([1,2,3,4,5]), e depois é feito um
laço "for" para exibir os itens do array um por um do indice 0 a (tamanho - 1).
*/